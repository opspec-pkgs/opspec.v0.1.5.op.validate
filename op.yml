description: validates an opspec package
name: github.com/opspec-pkgs/opspec.pkg.validate
inputs:
  pkg:
    dir:
      description: pkg to validate
run:
  container:
    cmd: [ pkg, validate, /pkg ]
    dirs: { /pkg: pkg }
    image: { ref: 'opctl/opctl:stable' }

