description: validates an opspec package
name: github.com/opspec-pkgs/opspec.op.validate
inputs:
  pkg:
    dir:
      description: pkg to validate
version: 1.0.0
run:
  container:
    cmd: [ opctl, pkg, validate, /pkg ]
    dirs:
      /pkg: $(pkg)
    image: { ref: 'opctl/opctl:stable' }

