description: validates an opspec operation
name: github.com/opspec-pkgs/opspec.op.validate
inputs:
  op:
    dir:
      description: operation to validate
version: 2.0.0
run:
  container:
    cmd: [ opctl, pkg, validate, /op ]
    dirs:
      /op: $(op)
    image: { ref: 'opctl/opctl:stable' }

