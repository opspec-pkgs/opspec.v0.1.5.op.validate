description: validates an opspec 0.1.3 package
name: github.com/opspec-pkgs/opspec0.1.3.pkg.validate
inputs:
  pkg:
    dir:
      description: pkg to validate
run:
  container:
    cmd: [ opctl, pkg, validate, /pkg ]
    dirs: { /pkg: pkg }
    image: { ref: 'opctl/opctl:stable' }

